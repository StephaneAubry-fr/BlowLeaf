import { Routes } from '@angular/router';
import { GardenCtrlComponent } from './component/garden-ctrl/garden-ctrl.component';

export const routes: Routes = [
  { path: '', component: GardenCtrlComponent },
];
